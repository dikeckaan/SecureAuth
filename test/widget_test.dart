import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Bu basit bir smoke test'tir.
    // Gerçek testler için mock servisleri ve daha kapsamlı test senaryoları eklenmelidir.
    expect(true, true);
  });
}
